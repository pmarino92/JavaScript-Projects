function myFunction() { // Creating a function to display a sentence
    var sentence = "I am learning"; // assigning variable "sentence"
    sentence += "a lot from this book!";// Concatenating with variable "sentence"
    document.getElementById("demo").innerHTML = sentence; // retrieving full sentence once function is done
}